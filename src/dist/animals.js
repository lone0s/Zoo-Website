const Animal = (name, desc, coral, ) => {
    return {
        name : String,
        description : String,
        coral : undefined,
    }
}
;

const Coral = (name, x, y) => {
    return {
        name : name,
        x : x,
        y : y,
    }
}
